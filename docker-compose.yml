version: '3.9'

services:
  app:
    build: .
    container_name: safe_deploy_app
    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - ./storage:/app/storage

    restart: always
